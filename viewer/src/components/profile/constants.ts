/** Matches Core `set_user_description` trim limit (see handlers_branding). */
export const MOCK_PROFILE_BIO_MAX_CHARS = 2048;

/**
 * Pre-filled when opening “Add a bio” in the viewer shell — long-form copy for layout and save-flow testing.
 * Safe to delete or replace before saving.
 */
export const MOCK_PROFILE_BIO_DEV_SAMPLE = `I design module boundaries that people can actually reason about: typed wire surfaces, explicit org scopes, and flows that do not make you open a ticket just to fix your @handle.

Most of my time goes into Core integration — genesis paths, registry hooks, and the unglamorous glue between “it compiles” and “it behaves the same way on-chain.” I care a lot about discoverability without turning the public profile into a billboard.

On the side I poke at storage layouts, mock shells, and preview UIs that are honest enough to surface UX failures before they become mainnet surprises.

Collaborating on specs, interop, and operator tooling — reach out if you are building. Not taking recruiter blasts; builders and serious operators only.`;
