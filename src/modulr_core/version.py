"""Release and protocol module version (YYYY.MM.DD.N).

Keep in sync with ``[project].version`` in ``pyproject.toml`` until automated.
"""

MODULE_VERSION = "2026.03.22.0"

__version__ = MODULE_VERSION
